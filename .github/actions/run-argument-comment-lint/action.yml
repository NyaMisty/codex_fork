name: Run argument comment lint
description: Run argument-comment-lint on codex-rs via Bazel.

inputs:
  target:
    description: Runner target passed to setup-bazel-ci.
    required: true
  buildbuddy-api-key:
    description: BuildBuddy API key used by Bazel CI.
    required: false
    default: ""

runs:
  using: composite
  steps:
    - name: Check whether Bazel-backed lint should run
      id: bazel_lint_gate
      env:
        BUILDBUDDY_API_KEY: ${{ inputs.buildbuddy-api-key }}
      shell: bash
      run: |
        if [[ -n "${BUILDBUDDY_API_KEY:-}" ]]; then
          echo "run=true" >> "$GITHUB_OUTPUT"
          exit 0
        fi

        echo "Skipping Bazel-backed argument-comment-lint without BuildBuddy credentials; fork PRs use cargo-native CI plus the binary build artifact."
        echo "run=false" >> "$GITHUB_OUTPUT"

    - uses: ./.github/actions/setup-bazel-ci
      if: ${{ steps.bazel_lint_gate.outputs.run == 'true' }}
      with:
        target: ${{ inputs.target }}
        install-test-prereqs: true

    - name: Install Linux sandbox build dependencies
      if: ${{ steps.bazel_lint_gate.outputs.run == 'true' && runner.os == 'Linux' }}
      shell: bash
      run: |
        sudo DEBIAN_FRONTEND=noninteractive apt-get update
        sudo DEBIAN_FRONTEND=noninteractive apt-get install -y --no-install-recommends pkg-config libcap-dev

    - name: Run argument comment lint on codex-rs via Bazel
      if: ${{ steps.bazel_lint_gate.outputs.run == 'true' && runner.os != 'Windows' }}
      env:
        BUILDBUDDY_API_KEY: ${{ inputs.buildbuddy-api-key }}
      shell: bash
      run: |
        bazel_targets="$(./tools/argument-comment-lint/list-bazel-targets.sh)"
        ./.github/scripts/run-bazel-ci.sh \
          -- \
          build \
          --config=argument-comment-lint \
          --keep_going \
          --build_metadata=COMMIT_SHA=${GITHUB_SHA} \
          -- \
          ${bazel_targets}

    - name: Run argument comment lint on codex-rs via Bazel
      if: ${{ steps.bazel_lint_gate.outputs.run == 'true' && runner.os == 'Windows' }}
      env:
        BUILDBUDDY_API_KEY: ${{ inputs.buildbuddy-api-key }}
      shell: bash
      run: |
        ./.github/scripts/run-argument-comment-lint-bazel.sh \
          --config=argument-comment-lint \
          --platforms=//:local_windows \
          --keep_going \
          --build_metadata=COMMIT_SHA=${GITHUB_SHA}
